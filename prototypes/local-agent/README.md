# Local Node Agent Prototype

该目录保留当前基于 Node 的本地 AI 原型服务。

说明：

- 这是从单前端原型阶段演进出来的过渡实现
- 当前用于本地开发时给 `frontend` 提供 `/api/ai/rewrite` 能力
- 长期主线不再是该实现，后续会由模板对齐的 Python backend + LangChain 实现替代
- 在新的目标架构中，该目录仅作为历史原型与迁移参考
