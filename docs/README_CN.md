# Writing IDE MVP

这是一个基于 React + Tiptap 的 Docs-as-Code AI 写作 IDE 原型，实现了 PRD 中 `V0 + V1` 的核心闭环：

- 三栏式写作工作台
- 基于选区的 AI 改写建议
- diff 预览与 accept / reject / retry
- 文档保存与 Markdown 导出预览
- 手动版本快照、版本差异查看与恢复

## 启动方式

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```
