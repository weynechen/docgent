# SECURITY

## 当前边界

- 默认不上传本地全文到云端
- 仅在用户触发 AI 请求时发送选区及必要上下文
- 当前项目没有正式后端与数据库

## 风险点

1. 未来接入真实模型服务时，需要明确数据发送范围
2. 图片上传能力若保留，需要增加文件类型与大小约束审查
3. 本地文件系统接入后，需要避免误覆盖和路径越权

## 当前原则

1. AI 请求必须最小化上下文
2. 用户应知道哪些内容会发给模型
3. 不在 UI 中提前暴露超出当前安全边界的承诺
